# Test Environment

Basic secret for validating the module works.

## Usage

```bash
terraform init
terraform plan
terraform apply
```

## Cleanup

```bash
terraform destroy
```

Note: `recovery_window_days = 0` allows immediate deletion.
